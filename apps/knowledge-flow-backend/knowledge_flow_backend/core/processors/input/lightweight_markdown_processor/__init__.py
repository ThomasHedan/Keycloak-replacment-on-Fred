"""Lightweight Markdown extractors (for conversational use).

These extractors avoid heavy conversions and dependencies, returning a compact
Markdown string suitable for injecting into an agent's context. They do not
write to disk and are not part of the ingestion pipeline.
"""
